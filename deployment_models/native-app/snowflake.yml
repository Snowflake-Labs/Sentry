# This is a project definition file, a required component if you intend to use Snowflake CLI in a project directory such as this template.

definition_version: 1
native_app:
  name: sentry
  application:
    name: sentry # This overrides the name of the application when it's autogenerated during `snow app run`
  source_stage: app_src.stage
  artifacts:
    - src: app/*
      dest: ./
    - src: ../../src
      dest: ./streamlit
